export enum EPaymentStatus {
    FREE = 'free',
    PENDING = 'pending',
    AWAITING_DELIVERY_CALC = 'awaiting_delivery_calc',
    PENDING_MASTER = 'pending_master',
    PROCESSING = 'processing',
    SUCCEEDED = 'succeeded',
    FAILED = 'failed',
    REFUNDED = 'refunded',
}
