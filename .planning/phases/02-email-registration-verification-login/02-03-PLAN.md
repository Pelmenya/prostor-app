---
phase: 02-email-registration-verification-login
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
    - src/views/auth/ui/verify-email-page.tsx
    - src/views/auth/ui/verify-email-page.test.tsx
    - src/views/profile/ui/profile-page.tsx
    - src/views/profile/ui/profile-page.test.tsx
autonomous: true
requirements: [REG-04, VERIFY-01, VERIFY-02, VERIFY-03]

must_haves:
    truths:
        - 'Переход по /verify-email?token=... вызывает POST /auth/verify-email и при успехе показывает «Почта подтверждена» (VERIFY-01/VERIFY-02)'
        - 'Авторизованный пользователь может из личного кабинета повторно запросить письмо подтверждения (VERIFY-03), кнопка disabled во время запроса'
        - 'Личный кабинет рендерится для пользователя без поля email-верификации — доступ не блокируется статусом почты (REG-04)'
    artifacts:
        - src/views/auth/ui/verify-email-page.tsx
        - src/views/profile/ui/profile-page.tsx
    key_links:
        - "verify-email STATUS_CONFIG.verified.title === 'Почта подтверждена'"
        - 'profile-page кнопка «Отправить письмо повторно» → resendVerification(accessToken) с локальным pending-стейтом'
---

<objective>
Слайс «подтверждение почты»: verify-email-страница уже потребляет ссылку и дёргает
POST /auth/verify-email (VERIFY-01) — правим copy-drift заголовка на locked «Почта
подтверждена» (VERIFY-02) вместе с тестом в одном коммите. Добавляем недостающую
точку входа для VERIFY-03 (resendVerification уже экспортирован, но не вызывается
нигде) — новая строка в личном кабинете. И фиксируем регрессией REG-04: кабинет не
блокируется статусом верификации (в TUser нет такого поля — гейта не существует).

Пользовательская история фазы (ROADMAP): **As a** new or returning user, **I want to**
register by email, verify my email, and log in by email or password, **so that** I land
in my authenticated personal cabinet through a shared auth screen that Phase 3 will
extend with Telegram login.

Слайс этого плана: пользователь подтверждает почту по ссылке и видит корректное
сообщение, может переотправить письмо из кабинета, и при этом неподтверждённая почта
никогда не преграждает ему вход в кабинет.

Purpose: Закрыть VERIFY-02 (copy) и VERIFY-03 (нет UI) минимальным диффом; зафиксировать
регрессией намеренное отсутствие email-verification-гейта (REG-04).
Output: Правка copy verify-email + теста; новая resend-строка в profile-page; новый
profile-page.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-email-registration-verification-login/02-RESEARCH.md
@.planning/phases/02-email-registration-verification-login/02-PATTERNS.md
@.planning/phases/02-email-registration-verification-login/02-UI-SPEC.md

@src/views/auth/ui/verify-email-page.tsx
@src/views/profile/ui/profile-page.tsx
@src/features/auth/index.ts
@src/shared/model/t-user.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: VERIFY-02 — copy-fix «Почта подтверждена» + обновление теста (в одном коммите)</name>
  <files>src/views/auth/ui/verify-email-page.tsx, src/views/auth/ui/verify-email-page.test.tsx</files>
  <read_first>
    - src/views/auth/ui/verify-email-page.tsx (STATUS_CONFIG.verified.title, строка 15)
    - src/views/auth/ui/verify-email-page.test.tsx (утверждение, строка ~82: getByText('Email подтверждён'))
    - 02-UI-SPEC.md Copywriting Contract (locked «Почта подтверждена»)
  </read_first>
  <action>
    В verify-email-page.tsx изменить STATUS_CONFIG.verified.title c 'Email подтверждён' на locked-копию 'Почта подтверждена'. В том же изменении обновить утверждение теста verify-email-page.test.tsx (строка ~82) с getByText('Email подтверждён') на getByText('Почта подтверждена'). Copy-fix и тест правятся вместе (RESEARCH Pitfall 3 — не разносить по волнам, иначе CI покраснеет как ложная регрессия). Ветку changed.title ('Email изменён') НЕ трогать — это другой сценарий, не в скоупе VERIFY-02. VERIFY-01 (verifyEmail на mount) уже покрыт существующим тестом — не менять.
  </action>
  <verify>
    <automated>npm run test -- src/views/auth/ui/verify-email-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    verify-email при успехе показывает «Почта подтверждена»; тест утверждает новую строку и зелёный; сценарий emailChanged не затронут; VERIFY-01-тест по-прежнему зелёный.
  </acceptance_criteria>
  <done>VERIFY-02: заголовок «Почта подтверждена» + тест обновлён в одном коммите; VERIFY-01 зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VERIFY-03 resend-строка + REG-04 регрессия в личном кабинете</name>
  <files>src/views/profile/ui/profile-page.test.tsx, src/views/profile/ui/profile-page.tsx</files>
  <read_first>
    - src/views/profile/ui/profile-page.tsx (существующие строки «Изменить почту» 42-55, «Сменить пароль» 57-65; импорты — EnvelopeIcon уже импортирован строка 4, useAuthStore строка 5)
    - src/features/auth/index.ts (resendVerification уже в public API, строка 9)
    - src/shared/model/t-user.ts (подтвердить отсутствие emailVerified/is_verified — обоснование REG-04)
    - 02-PATTERNS.md раздел profile-page.tsx (target-паттерн resend-строки, локальный isSending/resendResult)
    - src/views/auth/ui/login-page.test.tsx (образец мока useAuthStore и @/features/auth для нового profile-page.test.tsx)
  </read_first>
  <behavior>
    - VERIFY-03: клик по кнопке «Отправить письмо повторно» вызывает resendVerification с accessToken из стора; во время запроса кнопка disabled (повторный клик не шлёт второй запрос — RESEARCH Pitfall 4); при успехе виден текст «Письмо отправлено»; при ошибке resendVerification состояние success не показывается.
    - REG-04: ProfilePage рендерится для user-объекта без какого-либо поля email-верификации (нет гейта, блокирующего доступ по статусу почты) — присутствует «Личный кабинет» / имя пользователя.
  </behavior>
  <action>
    (1) Создать profile-page.test.tsx (файла нет). Мокать @/features/auth (resendVerification: vi.fn), useAuthStore (user с first_name/last_name, accessToken присутствует) по образцу login-page.test.tsx. Кейсы: (a) REG-04 — рендер ProfilePage с user без emailVerified-подобного поля показывает «Личный кабинет» и имя, компонент не возвращает null и не редиректит; (b) VERIFY-03 успех — клик по «Отправить письмо повторно» вызывает resendVerification(accessToken), после resolve виден «Письмо отправлено»; (c) VERIFY-03 pending/double-click — кнопка disabled пока запрос не разрешён, resendVerification вызван один раз; (d) VERIFY-03 ошибка — при reject «Письмо отправлено» не появляется. Использовать waitFor для async-состояний. Кейсы (b)-(d) падают (RED) до реализации строки в (2).
    (2) В profile-page.tsx добавить строку resend по target-паттерну из 02-PATTERNS.md: локальный useState isSending:boolean и resendResult:'idle'|'success'|'error'; accessToken через селектор useAuthStore((s) => s.accessToken); async handleResend() — guard на !accessToken||isSending, setIsSending(true), try resendVerification(accessToken)→setResendResult('success'), catch→setResendResult('error'), finally setIsSending(false). Импортировать resendVerification из '@/features/auth'; EnvelopeIcon уже импортирован — переиспользовать. Разметка строки — тот же shell «p-4 bg-base-100 rounded-2xl border border-base-content/10 flex items-center gap-4 w-full», но НЕ <Link> (действие на месте, без навигации): <EnvelopeIcon className="size-5 shrink-0" />, заголовок «Подтвердить почту», при resendResult==='success' — <p className="text-xs text-success">Письмо отправлено</p>; справа <button type="button" className="btn btn-sm btn-outline btn-primary" onClick={handleResend} disabled={isSending}> с текстом «Отправить письмо повторно» либо loading-спиннером при isSending. НЕ добавлять trailing PencilSquareIcon (нет навигации). НЕ гейтить строку на статус верификации — всегда доступное идемпотентное действие (UI-SPEC #3; в TUser нет поля верификации). Не менять существующие три строки кабинета.
  </action>
  <verify>
    <automated>npm run test -- src/views/profile/ui/profile-page.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    profile-page.test.tsx создан и зелёный: REG-04 (кабинет рендерится без email-verification-поля), VERIFY-03 (resend вызывает resendVerification с токеном, кнопка disabled во время запроса, «Письмо отправлено» при успехе, отсутствует при ошибке). Существующие три строки кабинета не тронуты; tsc проходит.
  </acceptance_criteria>
  <done>VERIFY-03 resend-строка работает (disabled во время запроса, success/error); REG-04 регрессия фиксирует отсутствие гейта; тесты зелёные.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                   | Description                                                                                                       |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| client → API (`POST /auth/verify-email`, `POST /auth/resend-verification`) | Токен верификации и запрос переотправки пересекают границу; валидность токена и rate-limiting — авторитет бэкенда |

## STRIDE Threat Register

| Threat ID | Category                                | Component                                     | Severity | Disposition | Mitigation Plan                                                                                                                                         |
| --------- | --------------------------------------- | --------------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-05   | Denial of Service                       | resendVerification (бюджет почтовой рассылки) | medium   | mitigate    | Клиент дизейблит кнопку во время in-flight запроса (предотвращает double-fire, RESEARCH Pitfall 4). Авторитетный rate-limiting — на бэкенде (transfer). |
| T-02-06   | Information Disclosure                  | verify-email error-state                      | low      | accept      | Уже показывается общее «Ссылка недействительна или истекла»; в этой фазе не меняется, доп. информации не раскрывает.                                    |
| T-02-07   | Elevation of Privilege / Access Control | REG-04 — доступ при неподтверждённой почте    | low      | accept      | Намеренное отсутствие email-verification-гейта по требованию REG-04; регрессионный тест фиксирует no-gating (кабинет рендерится без поля верификации).  |

</threat_model>

<verification>
- `npm run test -- src/views/auth/ui/verify-email-page.test.tsx src/views/profile/ui/profile-page.test.tsx` — зелёные.
- `npx tsc --noEmit` — типы profile-page корректны.
- Ручная проверка (Playwright): /verify-email?token=... при успехе → «Почта подтверждена»; в /profile клик «Отправить письмо повторно» → кнопка дизейблится, затем «Письмо отправлено»; кабинет открыт для неподтверждённого пользователя.
</verification>

<success_criteria>

- VERIFY-02: заголовок успеха — «Почта подтверждена», тест обновлён в том же изменении.
- VERIFY-03: строка resend вызывает resendVerification(accessToken), disabled во время запроса, success/error-состояния корректны.
- REG-04: ProfilePage рендерится без email-verification-гейта (регрессия зелёная).
- VERIFY-01 регрессия зелёная.
  </success_criteria>

<artifacts_produced>

- Изменённый `src/views/auth/ui/verify-email-page.tsx`: `STATUS_CONFIG.verified.title === 'Почта подтверждена'`.
- Изменённый `src/views/profile/ui/profile-page.tsx`: resend-строка «Подтвердить почту» с `handleResend`, локальными `isSending`/`resendResult`, вызовом `resendVerification`.
- Новый файл `src/views/profile/ui/profile-page.test.tsx` (REG-04 + VERIFY-03 кейсы).
- Обновлённое утверждение в `src/views/auth/ui/verify-email-page.test.tsx`.
  </artifacts_produced>

<output>
Create `.planning/phases/02-email-registration-verification-login/02-03-SUMMARY.md` when done
</output>
